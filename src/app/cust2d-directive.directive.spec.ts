import { Cust2dDirectiveDirective } from './cust2d-directive.directive';

describe('Cust2dDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Cust2dDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
